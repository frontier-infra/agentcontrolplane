node_modules/
dist/
.DS_Store
*.log
# local orchestration/HUD tooling state — never part of the spec
.omx/
# never commit signing keys
*.key
*.pem
secrets/
# internal / working docs — this repo is PUBLIC-facing; keep these local-only
HANDOFF.md
internal/
*.internal.md
NOTES.md
TODO.local.md
